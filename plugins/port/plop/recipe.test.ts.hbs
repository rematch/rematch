import { init } from '../../../../src'
import plugin from './{{pluginName}}'

describe('{{camelCase pluginName}}:', () => {
	test('creates a store with ported plugin', () => {
		const start = () =>
			init({
				plugins: [plugin()],
			})
		expect(start).not.toThrow()
	})
})
