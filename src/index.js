import init from './init'
import model from './model'
import { dispatch } from './dispatch'
import { select } from './select'

export default {
  init,
  model,
  dispatch,
  select,
}

export {
  init,
  model,
  dispatch,
  select,
}
